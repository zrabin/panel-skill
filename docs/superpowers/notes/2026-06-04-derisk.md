# Phase 0 de-risk results (2026-06-04)

Gate outcome: **PASS** — proceed with build.

## Step 1 — Node preflight
`node v25.9.0`; `node:test` and `node:vm` both load. PASS (≥18).

## Step 2 — Substrate / args spike (run `wf_ce8efe52-aa5`, via scriptPath + args)
Result: `{ argsThreaded: false, firstAgentReturned: 7, conditionalSpawnRan: true, secondAgentReturned: 14 }`

- **`args` threading via `scriptPath` does NOT work** — confirms the finding from prior harness work.
  **Decision:** the `/panel` command uses the hardened `__ARGS`-embedding path only (no
  args-param branch). Hardening: `mktemp` in a `0700` dir, `chmod 600`, **unlink after the
  run** (the scratch may contain PII).
- Schema-constrained `agent()` works. Conditional spawn (Stage-4 pattern) works. Substrate
  fully supports the design.

## Step 3 — Hook contract (confirmed via claude-code-guide)
- **stdin:** includes `tool_name` (e.g. `"Write"`) and `tool_input.file_path` (absolute).
- **inject context:** stdout JSON `{"hookSpecificOutput":{"hookEventName":"PostToolUse","additionalContext":"…"}}`,
  exit 0. Surfaces to the model as a system reminder next turn (instructs, does not
  auto-exec). The Task 11 hook script matches this.
- **settings.json registration:** two formats exist in the wild (`matcher:"Write"` + nested
  `hooks:[…]` vs. `matcher:{toolName:"Write"}`). **Defer to the `update-config` skill at
  Task 12** to emit the currently-correct structure. Confirmed mechanism is what matters.

## Step 4 — Cost baseline
Dogfood run (`w7aqivmll`): ~790k tokens for 5 lenses + ~29 findings on a ~600-line spec.
`estimateTokens` is calibrated to land near this for a ~24k-char/5-lens run; threshold 700k.
